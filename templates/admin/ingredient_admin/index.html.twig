{% extends '_admin_layout.html.twig' %}

{% block title %}Admin Dashboard - Ingredients{% endblock %}

{% block page_title %}Ingredient Management{% endblock %}
{% block page_subtitle %}Manage your pizza ingredients{% endblock %}

{% block admin_content %}
<div class="flex justify-between items-center mb-8">
  <h2 class="text-2xl font-semibold text-gray-800">Ingredients</h2>
  <a href="{{ path('app_ingredient_admin_new') }}" 
     class="inline-flex items-center gap-2 bg-[#D62828] text-white font-semibold px-4 py-2 rounded-lg shadow hover:bg-red-700 transition">
    <span class="text-xl font-bold">+</span> Add New Ingredient
  </a>
</div>

<!-- Collapsible Sections for Each Type -->
<div class="space-y-4">
  
  <!-- Size Section -->
  <div class="bg-white rounded-lg shadow-lg overflow-hidden">
    <button type="button" 
            class="collapsible-header w-full flex items-center justify-between px-6 py-4 bg-gradient-to-r from-blue-500 to-blue-600 text-white font-bold text-lg hover:from-blue-600 hover:to-blue-700 transition"
            onclick="toggleCollapsible(this)">
      <span class="flex items-center gap-3">
        📏 Size
        {% set sizeCount = ingredientsByType['Size']|length %}
        <span class="bg-white text-blue-600 rounded-full px-3 py-1 text-sm font-semibold">{{ sizeCount }}</span>
      </span>
      <span class="text-2xl transition-transform" style="transform: rotate(0deg);">▼</span>
    </button>
    
    <div class="collapsible-content" style="max-height: 9999px; overflow: hidden; transition: max-height 0.3s ease;">
      {% if ingredientsByType['Size'] is empty %}
        <div class="px-6 py-8 text-center text-gray-500">
          <p>No size ingredients added yet.</p>
        </div>
      {% else %}
        <div class="p-6 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
          {% for ingredient in ingredientsByType['Size'] %}
            <div class="bg-gradient-to-br from-blue-50 to-blue-100 border border-blue-200 rounded-xl shadow-sm overflow-hidden hover:shadow-md transition p-4">
              <div class="flex justify-between items-start mb-3">
                <h3 class="text-lg font-semibold text-gray-900">{{ ingredient.name }}</h3>
                <span class="text-[#D62828] font-semibold text-lg">₱{{ ingredient.price|number_format(0) }}</span>
              </div>
              
              <div class="flex gap-2 mb-4">
                <span class="px-3 py-1 rounded-full bg-blue-200 text-blue-800 text-sm font-medium">{{ ingredient.type }}</span>
                {% if ingredient.isAvailable %}
                  <span class="px-3 py-1 rounded-full bg-green-100 text-green-700 text-sm font-medium">Available</span>
                {% else %}
                  <span class="px-3 py-1 rounded-full bg-red-100 text-red-700 text-sm font-medium">Unavailable</span>
                {% endif %}
              </div>
              
              <div class="space-y-3">
                {% if ingredient.createdBy %}
                  <div class="px-3 py-1 bg-blue-100 text-blue-700 rounded text-center text-sm font-medium">
                    👤 Created by: {{ ingredient.createdBy.username }}
                  </div>
                {% endif %}
                
                <div class="flex gap-2">
                  <a href="{{ path('app_ingredient_admin_edit', {'id': ingredient.id}) }}" 
                     class="flex-1 text-center border border-gray-300 rounded-lg py-2 text-gray-700 hover:bg-gray-100 transition text-sm font-medium">
                    ✏️ Edit
                  </a>
                  <form method="post" 
                        action="{{ path('app_ingredient_admin_delete', {'id': ingredient.id}) }}" 
                        onsubmit="return confirm('Are you sure?');"
                        class="flex-1">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ingredient.id) }}">
                    <button class="w-full border border-red-300 text-red-600 rounded-lg py-2 hover:bg-red-50 transition text-sm font-medium">
                      🗑️ Delete
                    </button>
                  </form>
                </div>
              </div>
            </div>
          {% endfor %}
        </div>
      {% endif %}
    </div>
  </div>

  <!-- Base Section -->
  <div class="bg-white rounded-lg shadow-lg overflow-hidden">
    <button type="button" 
            class="collapsible-header w-full flex items-center justify-between px-6 py-4 bg-gradient-to-r from-yellow-500 to-yellow-600 text-white font-bold text-lg hover:from-yellow-600 hover:to-yellow-700 transition"
            onclick="toggleCollapsible(this)">
      <span class="flex items-center gap-3">
        🍯 Base
        {% set baseCount = ingredientsByType['Base']|length %}
        <span class="bg-white text-yellow-600 rounded-full px-3 py-1 text-sm font-semibold">{{ baseCount }}</span>
      </span>
      <span class="text-2xl transition-transform" style="transform: rotate(0deg);">▼</span>
    </button>
    
    <div class="collapsible-content" style="max-height: 9999px; overflow: hidden; transition: max-height 0.3s ease;">
      {% if ingredientsByType['Base'] is empty %}
        <div class="px-6 py-8 text-center text-gray-500">
          <p>No base ingredients added yet.</p>
        </div>
      {% else %}
        <div class="p-6 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
          {% for ingredient in ingredientsByType['Base'] %}
            <div class="bg-gradient-to-br from-yellow-50 to-yellow-100 border border-yellow-200 rounded-xl shadow-sm overflow-hidden hover:shadow-md transition p-4">
              <div class="flex justify-between items-start mb-3">
                <h3 class="text-lg font-semibold text-gray-900">{{ ingredient.name }}</h3>
                <span class="text-[#D62828] font-semibold text-lg">₱{{ ingredient.price|number_format(0) }}</span>
              </div>
              
              <div class="flex gap-2 mb-4">
                <span class="px-3 py-1 rounded-full bg-yellow-200 text-yellow-800 text-sm font-medium">{{ ingredient.type }}</span>
                {% if ingredient.isAvailable %}
                  <span class="px-3 py-1 rounded-full bg-green-100 text-green-700 text-sm font-medium">Available</span>
                {% else %}
                  <span class="px-3 py-1 rounded-full bg-red-100 text-red-700 text-sm font-medium">Unavailable</span>
                {% endif %}
              </div>
              
              <div class="space-y-3">
                {% if ingredient.createdBy %}
                  <div class="px-3 py-1 bg-blue-100 text-blue-700 rounded text-center text-sm font-medium">
                    👤 Created by: {{ ingredient.createdBy.username }}
                  </div>
                {% endif %}
                
                <div class="flex gap-2">
                  <a href="{{ path('app_ingredient_admin_edit', {'id': ingredient.id}) }}" 
                     class="flex-1 text-center border border-gray-300 rounded-lg py-2 text-gray-700 hover:bg-gray-100 transition text-sm font-medium">
                    ✏️ Edit
                  </a>
                  <form method="post" 
                        action="{{ path('app_ingredient_admin_delete', {'id': ingredient.id}) }}" 
                        onsubmit="return confirm('Are you sure?');"
                        class="flex-1">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ingredient.id) }}">
                    <button class="w-full border border-red-300 text-red-600 rounded-lg py-2 hover:bg-red-50 transition text-sm font-medium">
                      🗑️ Delete
                    </button>
                  </form>
                </div>
              </div>
            </div>
          {% endfor %}
        </div>
      {% endif %}
    </div>
  </div>

  <!-- Cheese Section -->
  <div class="bg-white rounded-lg shadow-lg overflow-hidden">
    <button type="button" 
            class="collapsible-header w-full flex items-center justify-between px-6 py-4 bg-gradient-to-r from-orange-500 to-orange-600 text-white font-bold text-lg hover:from-orange-600 hover:to-orange-700 transition"
            onclick="toggleCollapsible(this)">
      <span class="flex items-center gap-3">
        🧀 Cheese
        {% set cheeseCount = ingredientsByType['Cheese']|length %}
        <span class="bg-white text-orange-600 rounded-full px-3 py-1 text-sm font-semibold">{{ cheeseCount }}</span>
      </span>
      <span class="text-2xl transition-transform" style="transform: rotate(0deg);">▼</span>
    </button>
    
    <div class="collapsible-content" style="max-height: 9999px; overflow: hidden; transition: max-height 0.3s ease;">
      {% if ingredientsByType['Cheese'] is empty %}
        <div class="px-6 py-8 text-center text-gray-500">
          <p>No cheese ingredients added yet.</p>
        </div>
      {% else %}
        <div class="p-6 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
          {% for ingredient in ingredientsByType['Cheese'] %}
            <div class="bg-gradient-to-br from-orange-50 to-orange-100 border border-orange-200 rounded-xl shadow-sm overflow-hidden hover:shadow-md transition p-4">
              <div class="flex justify-between items-start mb-3">
                <h3 class="text-lg font-semibold text-gray-900">{{ ingredient.name }}</h3>
                <span class="text-[#D62828] font-semibold text-lg">₱{{ ingredient.price|number_format(0) }}</span>
              </div>
              
              <div class="flex gap-2 mb-4">
                <span class="px-3 py-1 rounded-full bg-orange-200 text-orange-800 text-sm font-medium">{{ ingredient.type }}</span>
                {% if ingredient.isAvailable %}
                  <span class="px-3 py-1 rounded-full bg-green-100 text-green-700 text-sm font-medium">Available</span>
                {% else %}
                  <span class="px-3 py-1 rounded-full bg-red-100 text-red-700 text-sm font-medium">Unavailable</span>
                {% endif %}
              </div>
              
              <div class="space-y-3">
                {% if ingredient.createdBy %}
                  <div class="px-3 py-1 bg-blue-100 text-blue-700 rounded text-center text-sm font-medium">
                    👤 Created by: {{ ingredient.createdBy.username }}
                  </div>
                {% endif %}
                
                <div class="flex gap-2">
                  <a href="{{ path('app_ingredient_admin_edit', {'id': ingredient.id}) }}" 
                     class="flex-1 text-center border border-gray-300 rounded-lg py-2 text-gray-700 hover:bg-gray-100 transition text-sm font-medium">
                    ✏️ Edit
                  </a>
                  <form method="post" 
                        action="{{ path('app_ingredient_admin_delete', {'id': ingredient.id}) }}" 
                        onsubmit="return confirm('Are you sure?');"
                        class="flex-1">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ingredient.id) }}">
                    <button class="w-full border border-red-300 text-red-600 rounded-lg py-2 hover:bg-red-50 transition text-sm font-medium">
                      🗑️ Delete
                    </button>
                  </form>
                </div>
              </div>
            </div>
          {% endfor %}
        </div>
      {% endif %}
    </div>
  </div>

  <!-- Topping Section -->
  <div class="bg-white rounded-lg shadow-lg overflow-hidden">
    <button type="button" 
            class="collapsible-header w-full flex items-center justify-between px-6 py-4 bg-gradient-to-r from-green-500 to-green-600 text-white font-bold text-lg hover:from-green-600 hover:to-green-700 transition"
            onclick="toggleCollapsible(this)">
      <span class="flex items-center gap-3">
        🌶️ Topping
        {% set toppingCount = ingredientsByType['Topping']|length %}
        <span class="bg-white text-green-600 rounded-full px-3 py-1 text-sm font-semibold">{{ toppingCount }}</span>
      </span>
      <span class="text-2xl transition-transform" style="transform: rotate(0deg);">▼</span>
    </button>
    
    <div class="collapsible-content" style="max-height: 9999px; overflow: hidden; transition: max-height 0.3s ease;">
      {% if ingredientsByType['Topping'] is empty %}
        <div class="px-6 py-8 text-center text-gray-500">
          <p>No topping ingredients added yet.</p>
        </div>
      {% else %}
        <div class="p-6 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
          {% for ingredient in ingredientsByType['Topping'] %}
            <div class="bg-gradient-to-br from-green-50 to-green-100 border border-green-200 rounded-xl shadow-sm overflow-hidden hover:shadow-md transition p-4">
              <div class="flex justify-between items-start mb-3">
                <h3 class="text-lg font-semibold text-gray-900">{{ ingredient.name }}</h3>
                <span class="text-[#D62828] font-semibold text-lg">₱{{ ingredient.price|number_format(0) }}</span>
              </div>
              
              <div class="flex gap-2 mb-4">
                <span class="px-3 py-1 rounded-full bg-green-200 text-green-800 text-sm font-medium">{{ ingredient.type }}</span>
                {% if ingredient.isAvailable %}
                  <span class="px-3 py-1 rounded-full bg-green-100 text-green-700 text-sm font-medium">Available</span>
                {% else %}
                  <span class="px-3 py-1 rounded-full bg-red-100 text-red-700 text-sm font-medium">Unavailable</span>
                {% endif %}
              </div>
              
              <div class="space-y-3">
                {% if ingredient.createdBy %}
                  <div class="px-3 py-1 bg-blue-100 text-blue-700 rounded text-center text-sm font-medium">
                    👤 Created by: {{ ingredient.createdBy.username }}
                  </div>
                {% endif %}
                
                <div class="flex gap-2">
                  <a href="{{ path('app_ingredient_admin_edit', {'id': ingredient.id}) }}" 
                     class="flex-1 text-center border border-gray-300 rounded-lg py-2 text-gray-700 hover:bg-gray-100 transition text-sm font-medium">
                    ✏️ Edit
                  </a>
                  <form method="post" 
                        action="{{ path('app_ingredient_admin_delete', {'id': ingredient.id}) }}" 
                        onsubmit="return confirm('Are you sure?');"
                        class="flex-1">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ingredient.id) }}">
                    <button class="w-full border border-red-300 text-red-600 rounded-lg py-2 hover:bg-red-50 transition text-sm font-medium">
                      🗑️ Delete
                    </button>
                  </form>
                </div>
              </div>
            </div>
          {% endfor %}
        </div>
      {% endif %}
    </div>
  </div>

</div>

<!-- Collapse/Expand Toggle Script -->
<script>
  function toggleCollapsible(button) {
    const content = button.nextElementSibling;
    const arrow = button.querySelector('span:last-child');
    
    if (content.style.maxHeight === '0px' || content.style.maxHeight === '') {
      // Expand
      content.style.maxHeight = '9999px';
      arrow.style.transform = 'rotate(180deg)';
    } else {
      // Collapse
      content.style.maxHeight = '0px';
      arrow.style.transform = 'rotate(0deg)';
    }
  }
</script>

{% endblock %}
